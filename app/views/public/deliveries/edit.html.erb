
<div class="delivery-edit">
    <div class="delivery-form">
      <h3>配送先編集</h3></br>
		<% if @delivery.errors.any? %>
		  <div id = "error_explanation">
		    <h2><%= pluralize(@delivery.errors.count, "error") %> prohibited this product from being saved:</h2>
		    <ul><% @delivery.errors.full_messages.each do |message| %>
		    <li><%= message %></li>
		    <% end %>
		    </ul>
		  </div>
		<% end %>

      <%= render 'form', delivery: @delivery %>
    </div>
</div>
