<table class="">
	<thead>
		<th>ジャンル検索</th>
	</thead>
	<tbody>
		<% @genres.each do |genre| %>
			<% if genre.genre_status == "有効" %>
				<tr>
					<td><%= link_to genre.name, public_sweet_search_path(genre) %></td>
				</tr>
			<% end %>
		<% end %>
	</tbody>
</table>