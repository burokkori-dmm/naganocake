<div class="container">
<div class="row">
<h4><span style="background-color:#CCCCCC;">注文情報確認</span></h4>
 	<span>表示価格は全て税込です。</span>
<div class="row">
	<div class="col-xs-8">
		<table class="table table-bordered">
			<tr>
				<th class="order-show">商品名</th>
				<th class="order-show">単価</th>
				<th class="order-show">数量</th>
				<th class="order-show">小計</th>
			</tr>

		<% @items.each do |item| %>
			<tr>
				<td><%= item.sweet.name %></td>
				<td><%= addTax(item.sweet.tax_free_price) %></td>
				<td><%= item.peace %></td>
				<td><%= addTax(item.sweet.tax_free_price*item.peace) %></td>
			</tr>
		<% end %>
		</table>
	</div>

	<div class="col-xs-4">
		<table class="table table-bordered">
			<tr>
				<th class="order-show">送料</th>
				<td>800</td>
			</tr>
			<tr>
				<th class="order-show">商品合計</th>
				<td><%= addTax(@items.sum(:tax_price)) %></td>
			</tr>
			<tr>
				<th class="order-show">請求金額</th>
				<td><%= addTax(@items.sum(:tax_price))+800 %></td>
			</tr>
		</table>
	</div>
</div>
<table>
	<tr>
<th><h4 class="order-new">支払い方法</h4></th>
	<td><%= @order.payment_method %></td>
</tr>
<tr>
<th><h4 class="order-new">お届け先</h4></th>
	<td>〒<%= @order.postal_code %>
	<%= @order.address %><br>
	<%= @order.name %>  様</td>
</tr>
</table>

<div class="text-center">
	<%= link_to '注文を確定する', public_order_success_path, class:"btn btn-success" %>
</div>
</div>
</div>