
<div class="container">
	<div class="row">
		<h1>商品詳細</h1>
		<div class="col-xs-2">
			<%= attachment_image_tag @sweet, :image, :fill, 100, 100, format: 'jpeg' %>
		</div>
		<div class="col-xs-10">
			<table class="">
				<thead>
					<th>商品名</th>
					<th>商品紹介</th>
					<th>ジャンル</th>
					<th>価格</th>
					<th>販売ステータス</th>
				</thead>
				<tbody>
					<tr>
						<td><%= @sweet.name %></td>
						<td><%= @sweet.detail %></td>
						<td><%= @sweet.genre.name %></td>
						<td>税込価格(税抜価格)
						<%= (@sweet.tax_free_price * 1.1).to_i %>
						(<%= @sweet.tax_free_price %>)
						<%= @sweet.sale_status %></td>
					</tr>
				</tbody>
			</table>

			<%= link_to "編集する",  edit_admin_sweet_path(@sweet.id) %>
		</div>
	</div>
</div>